---
id: V-005
title: Write the fleet quickstart (docs/fleet-quickstart.md)
type: task
status: product-backlog
priority: high
role: developer
stage: null
story_points: 3
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["docs","fleet","milestone-V"]
acceptance_criteria: ["docs/fleet-quickstart.md exists, starts with an About-this-document section, and covers: prerequisites, tasks+fleet MCP servers, a concrete first dispatch (create ticket -\u003e sprint -\u003e fleet_dispatch -\u003e fleet_jobs poll -\u003e actuals), the permission flow, and store sync/recover","Only that one new file is added - no other file modified","Commands in the doc match the real tool names (tasks_task_*, fleet_fleet_* as surfaced in opencode; bare task_*/fleet_* on the wire)"]
created_at: 2026-08-27T01:14:18.304Z
updated_at: 2026-08-27T01:14:18.304Z
---

WORKER TICKET for the Milestone V validation run (V-004). You are running inside the task fleet: an isolated git worktree of the Hephaestus repository, dispatched through the headless engine (FleetControl -> opencode serve -> worktree -> merge back). Your job: write the fleet quickstart - the missing newcomer path that walks seed-ticket -> dispatch -> poll -> merge -> verify. Nothing today documents the headless flow; you produce it as a single new file docs/fleet-quickstart.md. Keep it lean (150-250 lines): prerequisites (JDK 21, git, opencode binary, built jars via build.ps1), the two MCP servers (tasks + fleet, from opencode.json), a concrete first run (create a ticket with task_create, plan a sprint, fleet_dispatch it, poll fleet_jobs, read actuals), the permission flow (fleet_permissions + fleet_permissions_answer, once/always/reject), store discipline (fleet_status_store / fleet_sync_store / fleet_recover_store after PULL_CONFLICT), and what runs where (store = ground truth, chat = peer, Eclipse optional). Do NOT modify any other file. Follow the project-manager-doc-about skill convention: start the file with an About-this-document section.

## History
{"ts":"2026-08-27T01:14:18.304Z","action":"created"}
