---
id: W-004
title: D: Navi core design (DESIGN.md)
type: task
status: in-progress
priority: critical
role: developer
stage: design
story_points: 3
sprint: S-06
epic: null
assignee: fleet
blocked: true
blocker: fleet: opencode POST /session/ses_fb4b1f40fffermP6u236m40Ysp/message timed out after 1200s (the server itself may still be healthy - raise the budget or check whether the session is stuck busy)
labels: ["regression","v-model","editor"]
acceptance_criteria: ["DESIGN.md exists under scratch/fleet-regression/editor/ with an About-this-document section","The transition table covers EVERY command from the pinned spec - gaps fail review","The edge-case matrix lists all six boundary cases with expected behavior","CoreState JSON serialization is specified field by field","Only files under scratch/fleet-regression/editor/ are touched"]
created_at: 2026-08-28T03:25:20.459Z
updated_at: 2026-08-29T02:57:17.167Z
---

V-model regression stage 4 of 6. Author scratch/fleet-regression/editor/DESIGN.md per ARCHITECTURE.md: the CoreState record shape (JSON round-trip), the apply() transition table (one row per command from the pinned spec: guard/mode, state effect, cursor rule), undo as full-state snapshots (cap 100), and the edge-case matrix: empty buffer, col clamping after edits, cursor at EOL/EOF, Backspace at col 0, Delete at EOL, PageUp/Down clamping.

PINNED SPEC (verbatim, product-owner authored; the binding table is fixed - do not paraphrase):
Navi editor v1: two modes NORMAL and INSERT, startup NORMAL with empty buffer. Bindings: arrows move cursor (Left/Right chars, Up/Down lines, col clamps to line length); Home/End jump to line start/end; Ctrl+Home/Ctrl+End to buffer start/end; PageUp/PageDown move 12 lines; i or Insert enters INSERT at cursor; in INSERT printable characters insert at cursor, Enter inserts a newline after cursor line, Backspace deletes left char or joins previous line when col=0, Delete deletes right char or joins next line at EOL; Esc returns to NORMAL; in NORMAL x deletes char at cursor, d deletes current line, u undoes the last change (full-state snapshots, stack max 100), Ctrl+S saves the buffer to the loaded path (no-op when no path), Ctrl+Q quits. The core is a pure function apply(state, key) -> state; CoreState = {lines: string[], cursor: {line, col}, mode: NORMAL|INSERT, undoStack: CoreState[]} serialized as JSON. Work happens under scratch/fleet-regression/editor/ only.

## Comments
{"ts":"2026-08-29T02:16:39.880Z","by":"fleet","text":"launched into worktree opencode/W-004 by the fleet"}

## History
{"ts":"2026-08-28T03:25:20.459Z","action":"created"}
{"ts":"2026-08-28T03:25:21.286Z","action":"planned into S-06"}
{"ts":"2026-08-29T02:16:39.837Z","action":"updated:assignee,status"}
{"ts":"2026-08-29T02:57:17.167Z","action":"blocked:fleet: opencode POST /session/ses_fb4b1f40fffermP6u236m40Ysp/message timed out after 1200s (the server itself may still be healthy - raise the budget or check whether the session is stuck busy)","by":"fleet"}
