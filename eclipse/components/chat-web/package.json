{
  "name": "chat-web",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone chat renderer web component: static chat page + JS bridge contract, hostable in any host that can serve static files and call JS with string arguments.",
  "scripts": {
    "check": "node renderer-check.mjs && node bridge-check.mjs && node mermaid-check.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^24.0.0"
  },
  "dependencies": {
    "puppeteer-core": "^25.7.0"
  }
}
